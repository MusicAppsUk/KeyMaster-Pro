JACK VOICE WORKFLOW — drop-in

Goal: get this ONE file into your repo at exactly:
    .github/workflows/generate-voice-pack.yml
committed to the main branch.

EASIEST (works on tablet):
  1. GitHub repo (branch = main) -> Add file -> Create new file
  2. Name box, type exactly:  .github/workflows/generate-voice-pack.yml
  3. Open generate-voice-pack-workflow.txt from this zip, copy ALL, paste in
  4. Commit to main
  5. Actions tab -> "Generate Jack voice pack" -> Run (dry_run=true first, then false)

IF YOU EXTRACT THIS ZIP:
  The file is already at .github/workflows/generate-voice-pack.yml
  (some phones hide the .github folder — that's why the .txt copy is included)

The file is self-contained: the 17 lines + the generator are inside it.
Uses your secret ELEVENLABS_API_KEY. Defaults to Jack. Dry-run by default.
